function calculation(a,b) {
			var number_litre = a;
			var number_km = b;
			

			return (a/b)*100;
	};

		// 	document.getElementById("output").innerHTML = "<h2>The average consumption is " + result + "l and your car is a " + car +  ".</h2>";
		// }
		// document.getElementById("button").addEventListener("click", calculation, false)